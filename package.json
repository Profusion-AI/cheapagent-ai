{
  "name": "cheapagent-ai",
  "version": "0.2.12",
  "private": true,
  "description": "Browser-side context efficiency workbench for measuring and converting agent documents with doc2toon.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174",
    "build": "vite build && node scripts/apply-deploy-env.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 4174",
    "lint": "npm run build"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "@netlify/identity": "^1.2.0",
    "doc2toon": "^0.4.2"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=20"
  }
}
